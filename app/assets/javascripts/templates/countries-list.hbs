
<h3 class="c-title -normal">{{name}}</h3>

<div class="c-list-grid">
  {{#each countries as |country|}}
    <div class="item -1-2 -tablet-1-3">
      <a href="#">{{country.name}}</a>
    </div>
  {{/each}}
</div>
