
<h3 class="c-card-title -border-1 -color-5 -normal">{{name}}</h3>

<div class="c-list-grid">
  {{#each countries as |country|}}
    <div class="item -1-2 -tablet-1-3">
      <a href="/countries/{{country.iso}}">{{country.name}}</a>
    </div>
  {{/each}}
</div>
