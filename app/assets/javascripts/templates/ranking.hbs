<h2>{{name}}</h2>
{{#if data}}
  <table>
    <thead>
      <tr>
        <th>
          Country
        </th>
        <th class="-right">
          Value
        </th>
      </tr>
    </thead>
    <tbody>
      {{#each data}}
      <tr>
        <td><a href="/countries/{{this.iso3}}">{{this.country}}</a></td>
        <td class="-right">{{this.value}}</td>
      </li>
      {{/each}}
    </tbody>
  </table>
{{else}}
  <p>No data available</p>
{{/if}}
