{{#if groups}}
  {{#each groups as |countries region|}}
    <div class="l-content">
      <div class="wrapper">
        <div class="row collapse">
          <div class="column small-12">
            <h2>{{region}}</h2>
          </div>
        </div>
        <div class="row collapse">
          <div class="c-list -columns">
            {{#each countries as |country|}}
              <div class="column small-6 medium-3">
                <a class="item-link" href="/countries/{{country.country_iso_3}}?data=projects&sortby=title_asc&global=all&region={{country.region_iso}}&country={{country.country_iso_3}}">{{country.name}}</a>
              </div>
            {{/each}}
          </div>
        </div>
      </div>
    </div>
  {{/each}}
{{else}}
  <div class="l-content">
    <div class="wrapper">
      <div class="row collapse">
        <div class="column small-12">
          <p>No results.</p>
        </div>
      </div>
    </div>
  </div>
{{/if}}
