
<ul class="c-list -row-items">
  {{#each items as |item|}}
    <li class="list-item">
      <div class="c-avatar -small -ph">
        <img src="{{item.photo}}">
      </div>
      <a href="/network/{{item.id}}">{{item.name}}</a>
    </li>
  {{/each}}
</ul>
