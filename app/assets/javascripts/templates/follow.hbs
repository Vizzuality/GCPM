{{#if mobile}}
  {{#if followed}}
    <svg class="c-icon -normal -secondary">
      <use xlink:href="#icon-minus"></use>
    </svg>
  {{else}}
    <svg class="c-icon -normal -secondary">
      <use xlink:href="#icon-collaborators"></use>
    </svg>
  {{/if}}
{{else}}
  {{#if followed}}
    Unfollow
  {{else}}
    Follow
  {{/if}}
{{/if}}
