(function(App) {

  'use strict'

  <% template = @current_type == 'events' ? 'events/grid' : 'projects/grid'  %>
  var items = "<%= escape_javascript render(partial: template, locals: { items: @items, more: @more }) %>";
  document.getElementById('content').innerHTML = items;

})(this.App);
