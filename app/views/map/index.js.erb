(function(App) {
  'use strict'

  var nextProjects = "<%= escape_javascript(render partial: 'map/' + @current_type ) %>";
  document.getElementById('content').innerHTML += nextProjects;

})(this.App);
