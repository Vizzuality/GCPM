(function(App) {

  'use strict'

  var items = "<%= escape_javascript render(partial: 'projects/grid', locals: {items: @items}) %>";
  document.getElementById('content').innerHTML = items;

})(this.App);
