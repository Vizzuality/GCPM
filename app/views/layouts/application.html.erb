<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= @title ? @title + ' - ' : nil %>Global Cancer Project Map</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': true %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
    <%= render "shared/icons" %>
    <%= render "shared/header" %>
    <% if alert %>
      <%= alert %>
    <% else %>
      <% if notice %>
        <div class="c-notice">
          <%= notice %>
        </div>
      <% end %>
    <% end %>
    <%= yield %>
    <%= render "shared/footer" %>
    <%= render "shared/modal" %>
  </body>
</html>
