<div class="project_add">
  <%= simple_form_for [@user, @project] do |f| %>
    <%= render partial: f %>
    <span class="pre-submit"><%= f.button :submit, 'Update', class: 'f-submit -network'  %></span>
  <% end %>
</div>
