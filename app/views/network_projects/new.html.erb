<div class="project_add">
	<%= simple_form_for [@user, @project], url: user_projects_url(@user) do |f| %>
	  <%= render partial: f %>
	  <span class="pre-submit"><%= f.button :submit, 'Submit', class: 'f-submit -network' %></span>
	<% end %>
</div>
