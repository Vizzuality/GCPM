(function() {

  'use strict'

  <% template = @current_type %>
  var htmlContent = "<%= escape_javascript render(partial: template) %>";

  document.getElementById('content').innerHTML = htmlContent;

})();
